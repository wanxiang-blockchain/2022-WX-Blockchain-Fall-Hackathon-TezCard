import styled from '@emotion/styled';

export const DaoTabs = styled.div`
  texttransform: none;
  > div > div:nth-child(2) > div {
    padding: 0;
  }
`;
export const tabPanelStyle = {
  padding: 0,
};
export const AddDao = styled.div`
  filter: drop-shadow(0px 4px 4px rgba(0, 0, 0, 0.25));
  > img {
    dispaly: block;
    width: 50px;
    height: 50px;
  }
`;
